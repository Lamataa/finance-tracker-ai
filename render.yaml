services:
  - type: web
    name: finance-tracker
    env: python
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && uvicorn main_deploy:app --host 0.0.0.0 --port $PORT"